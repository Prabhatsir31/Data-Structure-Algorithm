import React, { useState } from 'react';
import '../index.css'; // Create a corresponding CSS file for styling

const Home = () => {
  return (
    <div className="home">
        Hello World
    </div>
  );
};

export default Home;
